import React from "react";

const Home = () => {
    return(
        <div>
            <h1>Home page a asle taka dite hbe</h1>
            <p>Now you can use React Router anywhere in your app! For a simple example, open src/App.js and replace the default markup with some routes</p>
        </div>
    );
};



export default Home;